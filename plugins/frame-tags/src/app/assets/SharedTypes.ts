/* Colour types */
export type ColourType = Record<string, string>;

export type RGBType = {
  r: number;
  g: number;
  b: number;
};

export type HSLType = {
  h: number;
  s: number;
  l: number;
};
